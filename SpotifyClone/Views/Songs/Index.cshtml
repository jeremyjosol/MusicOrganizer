@{
    Layout = "_Layout";
}

@model List<SpotifyClone.Models.Song>

<div class="container">
  @if (Model.Count == 0)
  {
    <p>No songs available.</p>
  }
  else
  {
    <ul>
      @foreach (SpotifyClone.Models.Song song in Model)
      {
        <li>@Html.SongLink(song)</li>
      }
    </ul>
  }
@HtmlActionLink("Add new Song", "Create")
</div>
