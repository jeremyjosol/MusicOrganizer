@{
    Layout = "_Layout";
}

@model List<SpotifyClone.Models.Song>

<div class="container">
  @if (Model.Count == 0)
  {
    <p>No songs available.</p>
  }
  else
  {
    <ul>
      @foreach (SpotifyClone.Models.Song song in Model)
      {
        <li>@Html.ActionLink(song.Title, "Details", new { id = song.SongId })</li>
      }
    </ul>
  }
@Html.ActionLink("Add new Song", "Create")
</div>
